{
    "name": "kubectl-deonly",
    "version": "1.0.0",
    "private": true,
    "description": "Use .deon files with kubectl to apply configurations to a Kubernetes cluster.",
    "license": "SEE LICENSE IN LICENSE",
    "scripts": {
        "test": "node --test \"tests/**/*.test.js\""
    }
}
